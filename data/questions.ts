import questionOverrides from "./question-overrides.json";

export type Difficulty = "Easy" | "Medium" | "Hard";

export type Rubric = {
  required: string[][];
  optional: string[][];
  misconceptions?: string[];
  minimumWords?: number;
};

export type InterviewQuestion = {
  id: number;
  category: string;
  difficulty: Difficulty;
  question: string;
  ideal: string;
  rubric: Rubric;
};

type Topic = Omit<InterviewQuestion, "id" | "question"> & {
  subject: string;
  baseQuestion: string;
};

const topic = (
  subject: string,
  category: string,
  difficulty: Difficulty,
  baseQuestion: string,
  ideal: string,
  required: string[][],
  optional: string[][] = [],
  misconceptions: string[] = [],
): Topic => ({
  subject,
  category,
  difficulty,
  baseQuestion,
  ideal,
  rubric: { required, optional, misconceptions, minimumWords: 12 },
});

// 73 hardware-engineering topics x 5 interview prompt styles = 365 daily questions.
// Each inner array is a concept group. Mentioning any phrase in a group earns that concept.
// Add synonyms to an existing group with commas; add a new group for another required idea.
const TOPICS: Topic[] = [
  topic("combinational and sequential logic", "Digital Logic", "Easy", "What is the difference between combinational logic and sequential logic?", "Combinational outputs depend only on current inputs. Sequential outputs also depend on stored state, normally held in flip-flops or registers and often updated by a clock.", [["current input", "present input"], ["stored state", "memory", "previous input"], ["flip-flop", "register"]], [["clock", "clocked"]]),
  topic("latches and flip-flops", "Digital Logic", "Easy", "Compare a latch with a flip-flop.", "A latch is level-sensitive and can change while its enable level is active. A flip-flop is edge-triggered and samples data on a clock edge, making synchronous timing easier to control.", [["level sensitive", "level-triggered"], ["edge triggered", "clock edge"], ["enable", "clock"]]),
  topic("setup and hold time", "Digital Logic", "Medium", "Explain setup time and hold time.", "Setup time is how long data must be stable before the active clock edge; hold time is how long it must remain stable afterward. Violations can cause metastability or unreliable capture.", [["before", "prior to"], ["after", "following"], ["stable", "unchanged"], ["metastability", "incorrect capture"]]),
  topic("metastability", "Digital Logic", "Medium", "What is metastability and how do designers reduce its risk?", "Metastability occurs when a storage element cannot quickly resolve to a valid logic level, commonly after a timing violation or asynchronous input. Synchronizer flip-flops reduce the probability of failure.", [["invalid logic", "unstable", "cannot resolve"], ["setup", "hold", "asynchronous"], ["synchronizer", "two flip-flop", "2 flip-flop"]]),
  topic("multiplexers", "Digital Logic", "Easy", "What does a multiplexer do?", "A multiplexer selects one of several data inputs and forwards it to one output according to select lines. It is useful for routing signals or choosing ALU operands.", [["select", "choose"], ["multiple inputs", "several inputs"], ["one output", "single output"], ["select line", "control signal"]]),
  topic("decoders and encoders", "Digital Logic", "Easy", "Compare a decoder with an encoder.", "A decoder maps an n-bit code to one of many outputs, while an encoder maps an active input into a compact binary code. A priority encoder resolves multiple active inputs by priority.", [["decoder", "one of", "outputs"], ["encoder", "binary code", "compact code"]], [["priority encoder"]]),
  topic("half adders and full adders", "Digital Logic", "Easy", "How does a full adder differ from a half adder?", "A half adder adds two bits and produces sum and carry. A full adder also accepts carry-in, allowing full adders to be chained for multi-bit addition.", [["two bits", "a and b"], ["sum"], ["carry"], ["carry in", "cin"]]),
  topic("propagation delay", "Digital Logic", "Medium", "What is propagation delay in a digital circuit?", "Propagation delay is the time between an input transition and the corresponding valid output transition. Long combinational paths limit the maximum safe clock frequency.", [["time", "delay"], ["input change", "input transition"], ["output change", "valid output"]], [["critical path", "clock frequency"]]),
  topic("Boolean simplification", "Digital Logic", "Easy", "Why simplify Boolean logic before implementation?", "Boolean simplification reduces gates and logic depth, usually lowering area, power, cost, and propagation delay while preserving the same truth table.", [["fewer gates", "reduce gates", "logic reduction"], ["same function", "same truth table", "equivalent"]], [["delay", "power", "area", "cost"]]),
  topic("Karnaugh maps", "Digital Logic", "Medium", "How does a Karnaugh map simplify logic?", "A K-map places truth-table outputs in Gray-code order so adjacent groups of powers of two eliminate changing variables and produce a simpler Boolean expression.", [["gray code", "adjacent"], ["group", "power of two", "1, 2, 4, 8"], ["eliminate", "remove variable", "simplify"]]),
  topic("finite-state machines", "Digital Logic", "Medium", "Describe the parts of a finite-state machine.", "An FSM has a state register, next-state combinational logic, and output logic. The next state depends on the current state and inputs and is loaded on a clock edge.", [["current state", "state register"], ["next state", "next-state logic"], ["input"], ["output"]], [["clock edge", "clock"]]),
  topic("Moore and Mealy machines", "Digital Logic", "Medium", "Compare Moore and Mealy finite-state machines.", "A Moore machine's output depends only on state. A Mealy machine's output depends on state and current inputs, so it can respond sooner but may need more care to avoid glitches.", [["Moore", "state only", "only on state"], ["Mealy", "state and input", "inputs and state"]], [["glitch", "respond sooner", "fewer states"]]),
  topic("synchronous and asynchronous reset", "Digital Logic", "Medium", "Compare synchronous and asynchronous reset.", "A synchronous reset takes effect only on the active clock edge. An asynchronous reset acts immediately regardless of the clock, but its release must be controlled to avoid timing or metastability problems.", [["synchronous", "clock edge"], ["asynchronous", "immediate", "regardless of clock"]], [["deassert", "release", "metastability"]]),
  topic("clock-domain crossing", "Digital Logic", "Hard", "How would you safely move a signal between clock domains?", "A single control bit commonly uses a two-flop synchronizer. Multi-bit data needs a handshake, Gray-coded counter, or asynchronous FIFO so related bits remain coherent.", [["clock domain", "different clock"], ["synchronizer", "two flop", "2 flop"], ["multi-bit", "bus", "data"]], [["handshake", "gray code", "asynchronous fifo"]]),
  topic("tri-state buffers", "Digital Logic", "Medium", "What is a tri-state buffer and where is it useful?", "A tri-state buffer can drive zero, one, or high impedance. High impedance disconnects it electrically so multiple devices can share a bus, provided only one drives at a time.", [["zero", "0"], ["one", "1"], ["high impedance", "hi-z", "z state"], ["shared bus", "multiple devices"]]),

  topic("CPU instruction cycle", "Computer Architecture", "Easy", "Walk through the CPU fetch-decode-execute cycle.", "The program counter selects an instruction, memory supplies it, the control unit decodes it, operands are read, the operation executes, and the result is written back while the PC advances or branches.", [["fetch", "instruction memory"], ["decode", "control unit"], ["execute", "alu"], ["program counter", "pc"]], [["write back", "operand"]]),
  topic("CPU registers", "Computer Architecture", "Easy", "Why does a CPU use registers?", "Registers are very small, fast storage inside the CPU for operands, addresses, instructions, and intermediate results. They avoid slower memory accesses during execution.", [["fast", "low latency"], ["inside cpu", "processor"], ["operand", "intermediate", "address"]]),
  topic("cache memory", "Computer Architecture", "Easy", "What does a CPU cache do and why is it fast?", "A cache keeps recently or frequently used memory blocks near the CPU. It is smaller and built from faster SRAM, exploiting temporal and spatial locality to reduce average access time.", [["frequent", "recent"], ["near cpu", "close to processor"], ["sram", "faster memory"], ["latency", "access time"]], [["temporal locality", "spatial locality"]]),
  topic("cache locality", "Computer Architecture", "Medium", "Explain temporal and spatial locality.", "Temporal locality means recently accessed data is likely to be reused. Spatial locality means nearby addresses are likely to be accessed, which is why caches transfer blocks or cache lines.", [["temporal", "reused", "recent"], ["spatial", "nearby", "adjacent address"], ["cache line", "block"]]),
  topic("cache misses", "Computer Architecture", "Medium", "Compare compulsory, capacity, and conflict cache misses.", "Compulsory misses are first accesses, capacity misses occur when the working set exceeds cache size, and conflict misses occur when mapping forces competing blocks into the same location.", [["compulsory", "first access", "cold"], ["capacity", "working set", "cache size"], ["conflict", "same set", "mapping"]]),
  topic("virtual memory", "Computer Architecture", "Medium", "What problem does virtual memory solve?", "Virtual memory gives each process a protected address space and maps virtual pages to physical memory through page tables. It enables isolation and can use storage when RAM is insufficient, with a performance penalty.", [["virtual address", "address space"], ["physical memory", "ram"], ["page table", "mapping"], ["protection", "isolation"]], [["page fault", "disk", "storage"]]),
  topic("pipelining", "Computer Architecture", "Medium", "How does pipelining improve CPU performance?", "Pipelining overlaps stages of different instructions to improve throughput, though not the latency of one instruction. Hazards can require forwarding, stalls, or branch prediction.", [["overlap", "simultaneous stages"], ["throughput"], ["instruction stages", "pipeline stages"]], [["hazard", "stall", "forwarding", "branch prediction"]]),
  topic("pipeline hazards", "Computer Architecture", "Hard", "What are the main CPU pipeline hazards?", "Structural hazards compete for hardware, data hazards involve dependencies between instructions, and control hazards arise from branches. Designers use extra resources, forwarding, stalls, and prediction.", [["structural"], ["data hazard", "dependency"], ["control hazard", "branch"]], [["forwarding", "stall", "prediction"]]),
  topic("branch prediction", "Computer Architecture", "Medium", "Why do processors use branch prediction?", "Branch prediction guesses a branch's direction and target so the pipeline stays busy. A correct guess improves throughput; a misprediction flushes speculative work and costs cycles.", [["guess", "predict"], ["branch direction", "target"], ["pipeline", "keep busy"], ["mispredict", "flush", "penalty"]]),
  topic("RISC and CISC", "Computer Architecture", "Medium", "Compare RISC and CISC architectures.", "RISC generally favors simpler, regular instructions and load/store operation, while CISC supports more complex instructions and addressing modes. Modern implementations often blend ideas, so the tradeoff is not absolute.", [["RISC", "simple instruction", "load store"], ["CISC", "complex instruction", "addressing mode"]], [["modern", "blend", "tradeoff"]]),
  topic("endianness", "Computer Architecture", "Easy", "What is endianness?", "Endianness is the byte order used for multi-byte values. Little-endian stores the least-significant byte at the lowest address; big-endian stores the most-significant byte there.", [["byte order"], ["little endian", "least significant byte"], ["big endian", "most significant byte"], ["lowest address"]]),
  topic("32-bit and 64-bit processors", "Computer Architecture", "Easy", "What commonly changes between 32-bit and 64-bit processors?", "The width affects registers, arithmetic, pointers, and address space. A 64-bit architecture can address much more memory, though actual limits depend on the implementation and operating system.", [["register", "word size"], ["pointer", "address"], ["more memory", "address space"]]),
  topic("microprocessors and microcontrollers", "Computer Architecture", "Easy", "Compare a microprocessor with a microcontroller.", "A microcontroller integrates a CPU, memory, timers, and peripherals on one chip for embedded control. A microprocessor usually relies more on external memory and peripherals and targets higher-performance systems.", [["microcontroller", "memory", "peripheral", "one chip"], ["microprocessor", "external memory", "external peripheral"]]),
  topic("memory-mapped I/O", "Computer Architecture", "Medium", "How does memory-mapped I/O work?", "Peripheral registers occupy addresses in the processor's memory space, so normal load and store instructions configure devices or read their status. Volatile access prevents unsafe compiler optimization.", [["peripheral register", "device register"], ["memory address", "address space"], ["load", "store", "read", "write"]], [["volatile"]]),
  topic("DMA", "Computer Architecture", "Medium", "What is direct memory access and why use it?", "DMA lets a controller transfer blocks between a peripheral and memory with little CPU involvement. The CPU configures the transfer and handles completion, freeing cycles for other work.", [["peripheral", "device"], ["memory", "ram"], ["without cpu", "little cpu", "cpu involvement"], ["transfer", "block"]]),
  topic("interrupt latency", "Computer Architecture", "Hard", "What contributes to interrupt latency?", "Interrupt latency includes finishing or pausing current work, priority and masking, pipeline effects, context saving, vector lookup, and entering the handler. Critical sections and disabled interrupts can increase it.", [["time", "delay"], ["interrupt request", "event"], ["handler", "isr"], ["context", "register save", "priority"]]),

  topic("polling and interrupts", "Embedded Systems", "Easy", "When would you use interrupts instead of polling?", "Polling repeatedly checks a device and is simple but consumes CPU time. Interrupts notify the CPU when an event occurs and are better for infrequent or time-sensitive events, though handlers add complexity.", [["polling", "repeatedly check"], ["interrupt", "event", "notify"], ["cpu time", "efficiency"]], [["latency", "handler", "isr"]]),
  topic("volatile in embedded C", "Embedded Systems", "Medium", "What does volatile mean in embedded C?", "Volatile tells the compiler that a value can change outside normal program flow, so accesses must actually occur. It is common for hardware registers and ISR-shared data but does not make operations atomic or thread-safe.", [["compiler", "optimization"], ["change externally", "outside program", "unexpectedly"], ["hardware register", "isr"]], [["not atomic", "not thread safe"]]),
  topic("GPIO configuration", "Embedded Systems", "Easy", "How do you configure and use a GPIO pin?", "Configure the pin's direction and electrical mode, then use its input or output register. Inputs may need pull resistors; outputs must respect voltage and current limits.", [["direction", "input", "output"], ["register", "configure"], ["read", "write"]], [["pull-up", "pull-down", "current limit"]]),
  topic("pull-up and pull-down resistors", "Embedded Systems", "Easy", "Why are pull-up or pull-down resistors used?", "They give an undriven digital input a defined default logic level instead of allowing it to float. The resistor limits current when another device drives the opposite level.", [["default", "known logic", "defined level"], ["floating", "float"], ["resistor", "limit current"]]),
  topic("switch debouncing", "Embedded Systems", "Medium", "How would you debounce a mechanical switch?", "Mechanical contacts bounce and create multiple transitions. Debouncing waits for a stable state using a timer or filtered sampling, or uses an RC network and Schmitt trigger in hardware.", [["bounce", "multiple transition"], ["stable", "delay", "timer", "sampling"], ["software", "hardware", "rc", "schmitt"]]),
  topic("UART", "Embedded Systems", "Easy", "How does UART communication work?", "UART is asynchronous serial communication using transmit and receive lines. Both ends agree on baud rate and frame format, typically start bit, data bits, optional parity, and stop bit.", [["asynchronous", "no clock"], ["tx", "transmit"], ["rx", "receive"], ["baud rate"], ["start bit", "stop bit"]]),
  topic("SPI", "Embedded Systems", "Medium", "Describe SPI and its tradeoffs.", "SPI is a synchronous, usually full-duplex bus with clock, controller-out/peripheral-in, peripheral-out/controller-in, and chip-select signals. It is fast but uses more wires and a select per peripheral.", [["synchronous", "clock"], ["mosi", "controller out", "data out"], ["miso", "controller in", "data in"], ["chip select", "cs"]], [["full duplex", "fast", "more wires"]]),
  topic("I2C", "Embedded Systems", "Medium", "Describe I2C and its tradeoffs.", "I2C is a synchronous addressed bus using shared SDA and SCL open-drain lines with pull-ups. Many devices share two wires, but it is generally slower and requires address management.", [["sda", "data line"], ["scl", "clock line"], ["address"], ["open drain", "pull-up"]], [["two wires", "multiple devices"]]),
  topic("SPI versus I2C", "Embedded Systems", "Medium", "When would you choose SPI instead of I2C?", "Choose SPI for higher speed, full duplex, simple framing, or low latency when extra wires are acceptable. Choose I2C when many addressed peripherals must share only two wires.", [["SPI", "speed", "full duplex"], ["I2C", "two wires", "address"], ["tradeoff", "extra wires", "many devices"]]),
  topic("timers and counters", "Embedded Systems", "Medium", "How are hardware timers used in embedded systems?", "A hardware timer counts clock ticks independently of normal code and can generate interrupts or outputs for delays, periodic scheduling, PWM, event counting, and input capture.", [["clock", "tick", "count"], ["independent", "hardware"], ["interrupt", "event"]], [["pwm", "periodic", "capture"]]),
  topic("PWM", "Embedded Systems", "Easy", "What is PWM and what can it control?", "Pulse-width modulation switches a digital output at a fixed frequency while varying duty cycle. The average delivered power changes, enabling motor speed, LED brightness, and filtered analog-like control.", [["pulse width", "switch"], ["duty cycle"], ["frequency"], ["average", "power"]], [["motor", "led", "filter"]]),
  topic("watchdog timers", "Embedded Systems", "Medium", "What is a watchdog timer?", "A watchdog is an independent timer software must periodically service. If software hangs and fails to service it, the watchdog resets or recovers the system.", [["timer"], ["periodically", "service", "feed", "kick"], ["hang", "failure"], ["reset", "recover"]]),
  topic("real-time systems", "Embedded Systems", "Medium", "What makes a system real-time?", "A real-time system is correct only if it produces the right result within a required deadline. Hard real-time systems cannot tolerate missed deadlines; soft real-time systems degrade when deadlines are missed.", [["deadline", "timing constraint"], ["correct result", "correctness"], ["hard real time", "hard deadline"], ["soft real time", "soft deadline"]]),
  topic("RTOS task scheduling", "Embedded Systems", "Hard", "How does an RTOS schedule tasks?", "An RTOS typically runs the highest-priority ready task and preempts lower-priority work. Tasks block on events or delays; designers must manage deadlines, shared resources, and priority inversion.", [["priority"], ["ready task", "scheduler"], ["preempt", "context switch"], ["block", "event", "delay"]], [["priority inversion", "mutex", "deadline"]]),
  topic("stack and heap", "Embedded Systems", "Medium", "Compare stack and heap memory in embedded software.", "The stack provides automatic, deterministic storage for calls and local variables but is limited and can overflow. The heap supports dynamic allocation but can fragment and make timing or failure less predictable.", [["stack", "local", "function call"], ["heap", "dynamic allocation"], ["overflow", "limited"], ["fragmentation", "nondeterministic", "unpredictable"]]),

  topic("Ohm's law", "Circuits", "Easy", "Explain Ohm's law and its limits.", "Ohm's law relates voltage, current, and resistance as V=IR for an ohmic element. It does not mean every nonlinear component has constant resistance under all conditions.", [["voltage"], ["current"], ["resistance"], ["v=ir", "V = I R"]], [["linear", "nonlinear"]]),
  topic("Kirchhoff's laws", "Circuits", "Easy", "State KCL and KVL.", "KCL says the algebraic sum of currents at a node is zero, reflecting charge conservation. KVL says the algebraic sum of voltages around a closed loop is zero, reflecting energy conservation.", [["KCL", "current", "node"], ["KVL", "voltage", "loop"], ["conservation"]]),
  topic("capacitors at DC steady state", "Circuits", "Easy", "How does an ideal capacitor behave at DC steady state?", "After transients settle, capacitor voltage is constant, so i=C dv/dt is zero and the ideal capacitor acts as an open circuit. It still stores energy equal to one-half C V squared.", [["steady state", "constant voltage"], ["open circuit", "zero current"], ["i=c", "dv/dt"]], [["energy", "one-half cv squared", "1/2 cv^2"]]),
  topic("inductors at DC steady state", "Circuits", "Easy", "How does an ideal inductor behave at DC steady state?", "After transients settle, inductor current is constant, so v=L di/dt is zero and the ideal inductor acts as a short circuit. It stores magnetic energy equal to one-half L I squared.", [["steady state", "constant current"], ["short circuit", "zero voltage"], ["v=l", "di/dt"]], [["energy", "one-half li squared", "1/2 li^2"]]),
  topic("Thevenin and Norton equivalents", "Circuits", "Medium", "Compare Thevenin and Norton equivalents.", "A linear two-terminal network can be represented by a Thevenin voltage in series with resistance or a Norton current in parallel with the same resistance. They convert using Vth=InRn.", [["Thevenin", "voltage source", "series"], ["Norton", "current source", "parallel"], ["same resistance", "equivalent resistance"]], [["vth", "in rn", "source transformation"]]),
  topic("maximum power transfer", "Circuits", "Medium", "What is the maximum-power-transfer condition?", "For a resistive DC network, load power is maximized when load resistance equals Thevenin resistance. For AC, the load impedance is the complex conjugate of the source impedance; maximum power is not maximum efficiency.", [["load resistance", "load impedance"], ["Thevenin resistance", "source impedance"], ["equal", "complex conjugate"]], [["efficiency", "50 percent"]]),
  topic("operational amplifier assumptions", "Circuits", "Medium", "What ideal op-amp assumptions help analyze negative-feedback circuits?", "An ideal op-amp has infinite open-loop gain and input impedance and zero output impedance. With stable negative feedback in the linear region, input currents are zero and the input voltages are approximately equal.", [["infinite gain", "high gain"], ["zero input current", "infinite input impedance"], ["input voltage equal", "virtual short"], ["negative feedback"]]),
  topic("diodes", "Circuits", "Easy", "How does a diode behave in a basic circuit model?", "A diode primarily conducts from anode to cathode when forward biased and blocks reverse current until breakdown. A silicon constant-drop model often approximates forward voltage near 0.7 V.", [["forward bias", "conduct"], ["reverse bias", "block"], ["anode", "cathode"]], [["0.7", "breakdown"]]),
  topic("MOSFET switching", "Circuits", "Medium", "How is a MOSFET used as a switch?", "Gate-to-source voltage controls the channel. With sufficient gate drive it operates in the low-resistance on state; below threshold it is off. Designers check gate charge, losses, voltage, current, and safe operating limits.", [["gate", "gate-source voltage", "vgs"], ["on", "low resistance"], ["off", "threshold"]], [["gate charge", "loss", "rating"]]),
  topic("decoupling capacitors", "Circuits", "Medium", "Why place decoupling capacitors near IC power pins?", "A nearby decoupling capacitor supplies short transient currents and shunts high-frequency noise, keeping the local supply stable. Short traces reduce parasitic inductance and improve effectiveness.", [["power pin", "supply"], ["transient current", "current demand"], ["noise", "high frequency"], ["close", "short trace"]]),

  topic("blocking and nonblocking assignments", "FPGA & HDL", "Medium", "When should Verilog use blocking versus nonblocking assignments?", "Use nonblocking assignments in clocked sequential logic so registers update together after the time step. Use blocking assignments in combinational procedural logic where statement order models calculation flow.", [["nonblocking", "<=", "sequential", "clocked"], ["blocking", "=", "combinational"]], [["race", "simultaneous update"]]),
  topic("combinational HDL completeness", "FPGA & HDL", "Medium", "How do you avoid unintended latches in combinational HDL?", "Assign every output on every possible path, commonly by setting defaults before a case or if statement. Use combinational constructs and include all branches so an output never needs to retain its old value.", [["all paths", "every branch"], ["default assignment", "default value"], ["retain", "latch"]]),
  topic("simulation and synthesis", "FPGA & HDL", "Easy", "What is the difference between HDL simulation and synthesis?", "Simulation executes the HDL model to verify behavior over time. Synthesis converts synthesizable HDL into gates and hardware resources under device and timing constraints.", [["simulation", "behavior", "test"], ["synthesis", "gates", "hardware"], ["synthesizable"]]),
  topic("testbenches", "FPGA & HDL", "Medium", "What should a useful HDL testbench contain?", "A testbench instantiates the design under test, drives clocks, resets, normal and corner-case stimuli, and automatically checks outputs with assertions or a scoreboard.", [["design under test", "dut", "instantiate"], ["stimulus", "inputs"], ["expected output", "check output", "assertion"]], [["corner case", "clock", "reset"]]),
  topic("timing constraints", "FPGA & HDL", "Hard", "Why are FPGA timing constraints necessary?", "Constraints describe clocks, I/O timing, and valid path exceptions so static timing analysis can verify setup and hold requirements. Without correct constraints, a design may pass simulation yet fail in hardware.", [["clock", "period"], ["setup", "hold"], ["static timing", "timing analysis"], ["simulation", "hardware"]]),
  topic("static timing analysis", "FPGA & HDL", "Hard", "What does static timing analysis check?", "STA analyzes timing paths without applying input vectors, comparing data arrival and required times for setup and hold across process, voltage, and temperature conditions.", [["timing path", "path"], ["arrival time"], ["required time"], ["setup", "hold"]], [["process", "voltage", "temperature", "corner"]]),
  topic("FPGA resources", "FPGA & HDL", "Medium", "What hardware resources are typically available inside an FPGA?", "FPGAs contain lookup tables, flip-flops, routing, clock networks, block RAM, DSP blocks, and I/O resources. Good RTL maps intended functions efficiently onto these resources.", [["lookup table", "LUT"], ["flip-flop", "register"], ["routing", "interconnect"]], [["block ram", "bram", "dsp", "clock network", "i/o"]]),
  topic("FPGA simulation-to-board failure", "FPGA & Debugging", "Medium", "Your FPGA works in simulation but not on the board. What do you check?", "Verify power, pin assignments, I/O standards, clock and reset, timing constraints, and the programmed bitstream. Then use on-chip logic analysis or test points to isolate the failing stage.", [["pin assignment", "constraints"], ["clock"], ["reset"], ["bitstream", "programmed"]], [["power", "i/o standard", "logic analyzer", "timing"]]),
  topic("synchronizers and asynchronous FIFOs", "FPGA & HDL", "Hard", "When would you use a synchronizer versus an asynchronous FIFO?", "Use a two-flop synchronizer for a slowly changing single-bit control. Use an asynchronous FIFO for multi-bit streaming data between unrelated clock domains because it preserves word coherence and handles rate differences.", [["single bit", "control"], ["two flop", "2 flop", "synchronizer"], ["multi-bit", "streaming data"], ["asynchronous fifo", "different clock"]]),

  topic("power-rail debugging", "Hardware Debugging", "Easy", "A new board does not power up. How do you debug it safely?", "Start with visual inspection and resistance-to-ground checks while unpowered. Use a current-limited supply, verify each rail and enable sequence, watch current draw and temperature, and isolate shorts before attaching expensive components.", [["visual inspection"], ["resistance", "short"], ["current limit", "current-limited"], ["voltage rail", "power rail"]], [["temperature", "sequence", "ground"]]),
  topic("signal-integrity debugging", "Hardware Debugging", "Hard", "How would you investigate ringing or corrupted high-speed signals?", "Check probing technique first, then impedance, termination, return paths, trace geometry, crosstalk, and edge rate. Compare waveforms near source and receiver and adjust termination or layout based on measurements.", [["probe", "measurement"], ["impedance", "termination"], ["return path", "ground"], ["ringing", "reflection"]], [["crosstalk", "edge rate", "layout"]]),
  topic("logic analyzer and oscilloscope choice", "Hardware Debugging", "Medium", "When would you use a logic analyzer instead of an oscilloscope?", "A logic analyzer is best for many digital channels, protocol decoding, and long event sequences. An oscilloscope shows analog voltage versus time and is needed for edge shape, noise, ringing, and timing quality.", [["logic analyzer", "digital", "many channels"], ["oscilloscope", "analog", "voltage"], ["protocol", "sequence"], ["noise", "ringing", "edge"]]),
  topic("datasheet review", "Hardware Debugging", "Easy", "What do you look for first in a new component datasheet?", "Start with absolute maximum ratings, recommended operating conditions, pinout, power and decoupling, interface levels and timing, then application circuits and layout guidance.", [["absolute maximum", "rating"], ["operating condition", "voltage"], ["pinout", "pin"], ["timing", "interface"]], [["decoupling", "layout", "application circuit"]]),
  topic("schematic review", "Hardware Debugging", "Medium", "How would you review a schematic before layout?", "Check power rails and sequencing, pin functions, voltage compatibility, pull resistors, decoupling, clocks, resets, programming access, protection, net labels, and unused-pin treatment against datasheets.", [["power", "rail"], ["datasheet", "pin"], ["voltage level", "compatibility"], ["decoupling"]], [["clock", "reset", "programming", "unused pin"]]),
  topic("PCB layout priorities", "Hardware Design", "Hard", "What are your priorities when laying out a mixed-signal PCB?", "Partition by function, control return-current paths, keep decoupling close, minimize critical loops, route high-speed and sensitive analog signals carefully, and keep noisy digital or power nodes away from analog sections.", [["partition", "separate"], ["return path", "ground"], ["decoupling", "power"], ["analog", "digital", "noise"]], [["critical loop", "high speed", "differential"]]),
  topic("technical debugging process", "Hardware Debugging", "Medium", "Describe a structured process for debugging an unknown hardware failure.", "Reproduce the failure, define expected behavior, inspect and verify power and clocks, divide the system into stages, form one hypothesis at a time, measure, record results, and confirm the root cause and fix.", [["reproduce"], ["expected", "specification"], ["measure", "probe"], ["isolate", "divide", "stage"], ["hypothesis", "root cause"]]),
  topic("explaining a hardware project", "Behavioral", "Medium", "How would you present a hardware project in an interview?", "Briefly state the problem and your responsibility, explain key design choices and constraints, describe a difficult failure and how you measured it, then quantify the verified result and what you learned.", [["problem", "goal"], ["my role", "responsibility", "I designed"], ["design choice", "tradeoff"], ["test", "measure", "debug"], ["result", "outcome"]]),
];

const PROMPTS = [
  (t: Topic) => t.baseQuestion,
  (t: Topic) => `Explain ${t.subject} as you would to a technical interviewer.`,
  (t: Topic) => `What are the key ideas an engineer should understand about ${t.subject}?`,
  (t: Topic) => `A teammate asks you about ${t.subject}. Give a concise but technically accurate explanation.`,
  (t: Topic) => `During a design review, you are asked to explain ${t.subject}. What would you say?`,
];

const generatedQuestions: InterviewQuestion[] = PROMPTS.flatMap((makePrompt, round) =>
  TOPICS.map((item, index) => ({
    id: round * TOPICS.length + index + 1,
    category: item.category,
    difficulty: item.difficulty,
    question: makePrompt(item),
    ideal: item.ideal,
    rubric: item.rubric,
  })),
);

type QuestionOverride = Omit<InterviewQuestion, "id">;
const overrides = questionOverrides as Record<string, QuestionOverride>;

export const QUESTIONS: InterviewQuestion[] = generatedQuestions.map((question) => {
  const override = overrides[String(question.id)];
  return override ? { id: question.id, ...override } : question;
});

if (QUESTIONS.length !== 365) {
  throw new Error(`Interviewdle must contain exactly 365 questions; found ${QUESTIONS.length}.`);
}

export function getQuestionById(id: number) {
  return QUESTIONS.find((question) => question.id === id);
}
